[project]
name = "pykbg"
version = "0.0.1"
authors = [{ name = "Baptiste Fontaine", email = "b@ptistefontaine.fr" }]
license = "MIT"
requires-python = ">=3.7,<3.15"
dependencies = [
    "requests>=2.22.0",
]
[dependency-groups]
dev = [
    "mypy>=1,<2",
    "responses>=0.10.6",
    "types-requests",
    "pytest>=7,<10",
    "ruff>=0.15.5",
]

[tool.uv]
package = false
